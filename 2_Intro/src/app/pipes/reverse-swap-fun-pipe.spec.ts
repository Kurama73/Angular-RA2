import { ReverseSwapFunPipe } from './reverse-swap-fun-pipe';

describe('ReverseSwapFunPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseSwapFunPipe();
    expect(pipe).toBeTruthy();
  });
});
